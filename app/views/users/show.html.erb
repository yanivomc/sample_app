<% provide(:title, @user.name) %>

<div class="row">
  <aside class="span4">
    <section>
      <h1>
		<%= gravatar_for @user %>
		<%= cookies[:remember_token] %>
		<%= @user.remember_token%>
		<%= @user.name %>
      </h1>
    </section>
  </aside>
</div>